# Required: auth token for the dashboard API
# Generate with: openssl rand -hex 32
DASHBOARD_AUTH_TOKEN=change-me

# Optional overrides
PORT=3000
NODE_ENV=production

# Log file paths (defaults match docker-compose volume mounts)
BACKUP_LOG_PATH=/logs/backup.log
AUTO_REBUILD_LOG_PATH=/logs/auto-rebuild.log
